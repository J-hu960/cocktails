import { Settings } from "react-native"

export const PublicRoutes ={
    LOGIN:'login',
    SIGNUP:'signup',
}

export const PrivateRoutes ={
    HOME:'/',
    FAV:'favorites',
    SETTINGS:'settings',
    ROOMS:'rooms',
}